---
pageable: true
per_page: 10
---
<!-- sets a max width optimum for reading -->
<div class="max-w-2xl pt-6 mx-auto px-6 pb-16">
  <h2 class="text-4xl mb-6 mt-4 font-serif text-center">Latest Posting</h2>
  <%= partial("article_card", locals: { article: blog.articles.first }) %>
</div>
<div class="bg-celadon">
  <div class="max-w-2xl pt-6 mx-auto px-6 pb-12">
    <h1 class="text-4xl text-center mb-4">Hi, I'm Ryan!</h1>
    <%= image_tag 'second_profile_pic.jpg', class: "grayscale w-96 mx-auto" %>
    <p class="mt-4 text-lg">
      You've found my little corner of the internet where I post book reviews.
      Professionally I'm a software programmer and in my free time a walkable city enthusiast.
      I write about that too.
    </p>
    <p class="mt-4 text-lg">
      Thanks for stopping by!
    </p>
  </div>
</div>
<div class="">
  <div class="max-w-2xl pt-6 mx-auto px-6 pb-16">
    <h2 class="text-4xl mb-12 mt-4 font-serif text-center">Posts</h2>
    <% if paginate && num_pages > 1 %>
      <p>Page <%= page_number %> of <%= num_pages %></p>
      <% if prev_page %>
        <p><%= link_to 'Previous page', prev_page %></p>
      <% end %>
    <% end %>
    <% page_articles.each_with_index do |article, i| %>
      <%= partial("article_card", locals: { article: article }) %>
    <% end %>
    <% if paginate %>
      <% if next_page %>
        <p><%= link_to 'Next page', next_page %></p>
      <% end %>
    <% end %>
  </div>
</div>
<div class="bg-alice-blue">
  <div class="max-w-2xl pt-6 mx-auto px-6 pb-20">
    <h2 class="text-4xl mb-12 mt-4 font-serif text-center">Tags</h2>
    <div class="grid grid-cols-3 gap-4">
      <% blog.tags.each do |tag| %>
        <%= link_to "#{tag.first} (#{tag[1].count})", tag_path(tag.first), class: "active-tag-pill" %>
      <% end %>
    </div>
  </div>
</div>
